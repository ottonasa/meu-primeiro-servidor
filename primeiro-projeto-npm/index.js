const lodash = require('lodash'); // Importar biblioteca completa
// const { uniq } = require('lodash') // --> Desestruturação (mesmo resultado)
const { arrayNumeros, arrayLetras } = require('./array');

// const array = [1, 2, 4, 6, 4, 9, 7, 1];

// const araryUnico = lodash.uniq(array);

// console.log(araryUnico);

// console.log(array)

console.log(arrayLetras, arrayNumeros)